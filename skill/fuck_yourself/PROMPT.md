# fuck_yourself 部署 Prompt

将以下内容发送给 Claude Code 即可部署:

---

## Prompt

请在 `~/.claude/skills/fuck_yourself/`（替换 `~` 为实际路径）创建一个 user-invocable skill，功能是在用户对产出提出方向性质疑时执行自我反省。

核心设计：
1. **前置判断**：区分方向性质疑（进入反省）、细节修订（直接改）、偏好表达（存 memory）
2. **七步流程**：还原现场（追溯决策链标记偏离点）→ 归因（五级优先级：规则缺失/模糊/冲突/过窄/执行偏差）→ 泛化（抽象为通用原则）→ 向用户确认 → 存档（备份 CLAUDE.md 到 `.claude/claude_history/`，附变更记录）→ 更新项目 CLAUDE.md → 沉淀到 memory
3. **质量约束**：新原则不针对单一场景特化、不与现有规则矛盾、可验证、足够简洁
4. **用户确认后才写入**，不可跳过确认步骤
